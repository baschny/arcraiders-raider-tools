# Deployment-only environment for `cd infra && npm run cdk -- deploy`.
#
# Create infra/.env.cdk from this file. Its values override infra/.env during
# CDK synthesis but are not used by the local API server. Explicit shell
# environment variables take precedence over both files.

SNAPSHOT_ALLOWED_EMAIL=ernst@baschny.de
